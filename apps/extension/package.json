{
  "name": "@onplan/extension",
  "version": "0.1.0",
  "private": true,
  "description": "OnPlan Chrome Extension for capturing and scheduling tasks",
  "scripts": {
    "build": "tsc && webpack --mode production",
    "dev": "tsc && webpack --mode development --watch",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.0.0",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "ts-loader": "^9.5.0",
    "copy-webpack-plugin": "^11.0.0"
  }
}
